# .sh files are a common way to create scripts in Linux and
# Unix environments. Shell scripts do not have one specific
# programming language; their lines are executed as command-line commands.
#
# You can read more about the topic, for example in the discussion
# "What's a .sh file?" (https://stackoverflow.com/a/32263486)
#
# Lines starting with # are comments, and the text on those lines
# is not executed. Other lines are executed in the command line.
#
# Remove the # character at the beginning of the last line so that
# the echo command on that line is executed when this script runs.
# After that, you can run this file in the terminal by typing
# ./01_hello_world.sh

# echo 'Hello world!'
