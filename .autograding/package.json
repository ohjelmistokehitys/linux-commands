{
  "scripts": {
    "test": "vitest run --reporter=json --reporter=default --output-file=results-vitest.json",
    "report": "npx --yes tsx src/prepare-report.ts results-vitest.json ../result.json"
  },
  "dependencies": {
    "vitest": "4",
    "zx": "lite"
  },
  "type": "module",
  "devDependencies": {
    "@types/node": "^25.9.1"
  }
}
